server.port=8081

# Default profile for local development. Override in production with SPRING_PROFILES_ACTIVE=prod.
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/take_a_break_web?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USER:anokas}
spring.datasource.password=${DB_PASS:Informatica_1706869}
spring.datasource.driver-class-name=${DATASOURCE_DRIVER:com.mysql.cj.jdbc.Driver}

spring.sql.init.mode=always
spring.sql.init.encoding=UTF-8

spring.thymeleaf.cache=false
spring.mvc.hiddenmethod.filter.enabled=true

logging.level.org.springframework.jdbc.core=INFO

app.canonical-host=${APP_CANONICAL_HOST:takeabreak.pt}
app.force-canonical-host=${APP_FORCE_CANONICAL_HOST:true}
